	//Make the object follow the path
path_start(PathTour1, 2, path_action_stop, true);

//Check if the object has reached the end of the path
//if(path_end(objectNPC)){
    //Change the path for the object to follow
   // path_start(PathTour2, objectNPC, 4, true);
	//}
	
	
//if (path_index == path_get_number(PathTour1)-1) {
    //path_start(PathTour2, objectNPC, path_action_stop, true);
//}


